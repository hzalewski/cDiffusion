% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffusion.R
\name{run_diffusion_sparse}
\alias{run_diffusion_sparse}
\title{Run sparse diffusion map}
\usage{
run_diffusion_sparse(
  data,
  k = 2,
  k_neighbors = 15,
  oversampling = 20,
  n_iter = 200
)
}
\arguments{
\item{data}{Numeric matrix or data.frame.}

\item{k}{Integer. Number of diffusion dimensions to return (default 2).}

\item{k_neighbors}{Integer. Number of nearest neighbors for sparse method (default 15).}

\item{oversampling}{Integer. Dimensions added for stability in randomized SVD algorithm (default 20).}

\item{n_iter}{Integer. Number of subspace iterations in randomized SVD algorithm (default 200).}
}
\value{
A diffmap object
}
\description{
Performs non-linear dimensionality reduction using a memory-efficient sparse k-NN method.
It is better suited for larger datasets than the exact dense method, as its memory 
usage scales linearly rather than quadratically.
Uses an adaptive Gaussian kernel (local sigma based on the distance to the k-th nearest neighbor).
}
\examples{
data <- matrix(runif(500), nrow = 100, ncol = 5)
optimal_sigma <- estimate_sigma(data)
model <- diffusion_sparse(data, sigma = optimal_sigma, k_neighbors = 10)

}
